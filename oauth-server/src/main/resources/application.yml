server:
  port: 9000

spring:
  application:
    name: oauth-server
  profiles:
    active: dev
#data:
#  mongodb:
#    uri: mongodb://${MONGODB_HOST}:${MONGODB_PORT}/?authSource=${MONGODB_DB}
#    database: ${MONGODB_DB}
logging:
  level:
    org.springframework.security: DEBUG

